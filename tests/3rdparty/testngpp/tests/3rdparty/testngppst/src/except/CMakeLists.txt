
IF(MSVC)
  ADD_DEFINITIONS( -D_CRT_SECURE_NO_WARNINGS )
ENDIF(MSVC)

SET(TESTNGPPST_SRCS
  Exception.cpp
  AssertionFailure.cpp 
)

ADD_LIBRARY(testngppst-except STATIC ${TESTNGPPST_SRCS})

INCLUDE_DIRECTORIES(BEFORE ${CMAKE_SOURCE_DIR}/include)

